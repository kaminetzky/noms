<h1>Meals</h1>

<ul>
  <% @meals.each do |meal| %>
    <li>
      <%= link_to "#{meal.food.name} (#{time_ago_in_words meal.consumed_on} ago)", meal %>
    </li>
  <% end %>
</ul>

<%= link_to "New meal", new_meal_path %>